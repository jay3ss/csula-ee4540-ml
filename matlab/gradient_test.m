%function [minimum] = gradient_test(X, C, epsilon, gamma, M)
%  % Returns the minimum of a function using the gradient descent algorithm
%  nn = size(C);
%  rows = nn(1);
%  columns = nn(2);
%  
%  
%end